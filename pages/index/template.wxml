<!-- 使用wxs -->
<wxs src="tool.wxs" module="tool"></wxs>

<template name="userInfo">
  <view class="usermotto" wx:for="{{tool(motto)}}" wx:key="{{index}}">
    <text class="user-motto">{{item}}</text>
  </view>
</template>